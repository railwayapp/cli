mutation ServiceInstanceUpdateSource(
  $environmentId: String!
  $serviceId: String!
  $rootDirectory: String
  $watchPatterns: [String!]
) {
  serviceInstanceUpdate(
    environmentId: $environmentId
    serviceId: $serviceId
    input: {
      rootDirectory: $rootDirectory
      watchPatterns: $watchPatterns
    }
  )
}
