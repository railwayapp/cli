mutation VolumeInstanceBackupDelete($volumeInstanceId: String!, $volumeInstanceBackupId: String!) {
  volumeInstanceBackupDelete(
    volumeInstanceId: $volumeInstanceId
    volumeInstanceBackupId: $volumeInstanceBackupId
  ) {
    workflowId
  }
}
