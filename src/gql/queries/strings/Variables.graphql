query Variables(
	$projectId: String!
	$environmentId: String!
	$serviceId: String
	$pluginId: String
) {
	variables(
		projectId: $projectId
		environmentId: $environmentId
		serviceId: $serviceId
		pluginId: $pluginId
	)
}
