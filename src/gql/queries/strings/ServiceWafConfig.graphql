query ServiceWafConfig($environmentId: String!, $serviceId: String!) {
  serviceInstance(environmentId: $environmentId, serviceId: $serviceId) {
    id
    edgeConfig {
      id
      enabled
      underAttackModeUntil
    }
  }
}
