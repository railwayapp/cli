query PrivateNetworks($environmentId: String!) {
  privateNetworks(environmentId: $environmentId) {
    publicId
    projectId
    environmentId
    name
    dnsName
    createdAt
    deletedAt
    tags
    networkId
  }
}
