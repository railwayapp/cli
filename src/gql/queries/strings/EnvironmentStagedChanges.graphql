query EnvironmentStagedChanges($environmentId: String!, $decryptVariables: Boolean) {
  environmentStagedChanges(environmentId: $environmentId) {
    id
    createdAt
    updatedAt
    status
    lastAppliedError
    patch(decryptVariables: $decryptVariables)
  }
}
