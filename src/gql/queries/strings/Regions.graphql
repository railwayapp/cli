query Regions($projectId: String) {
    regions(projectId: $projectId) {
        name
        region
        country
        location
        workspaceId
        deploymentConstraints {
            deprecationInfo {
                isDeprecated
                replacementRegion
            }
        }
    }
}
