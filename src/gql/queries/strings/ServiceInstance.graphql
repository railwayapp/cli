query ServiceInstance($environmentId: String!) {
  environment(id: $environmentId) {
    serviceInstances(first: 100) {
      edges {
        node {
          id
          serviceId
        }
      }
    }
  }
}
