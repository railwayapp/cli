query SshPublicKeys($workspaceId: String) {
  sshPublicKeys(first: 100, workspaceId: $workspaceId) {
    edges {
      node {
        id
        name
        publicKey
        fingerprint
        userId
        workspaceId
      }
    }
  }
}
